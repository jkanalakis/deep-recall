apiVersion: apps/v1
kind: Deployment
metadata:
  name: deep-recall-orchestrator
spec:
  template:
    spec:
      containers:
      - name: orchestrator
        resources:
          limits:
            cpu: "2"
            memory: 4Gi
          requests:
            cpu: "1"
            memory: 2Gi 